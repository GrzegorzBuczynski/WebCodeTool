services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    env_file:
      - ./config/.env
    volumes:
      - ./results:/app/results
      - ./config:/app/config
    restart: unless-stopped
